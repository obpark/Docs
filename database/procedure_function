* procedure
  * 서버에서 실행, 속도가 빠름
  * 리턴값유무: 선택(여러개 가능, out 파라미터로 처리)
  * select 문에서 실행 불가능
  * 독립된 로직을 처리 

* function 
  * 클라이언트에서 처리
  * 리턴값유무: 필수 (하나만 가능, in 파리미터만 가능)
  * select 문에서 실행 가능
  * 로직을 보조하는 경우
